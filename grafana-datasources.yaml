apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: logging-demo
  labels:
    grafana_datasource: "1"
data:
  loki-datasource.yaml: |
    apiVersion: 1
    datasources:
      - name: Loki
        type: loki
        access: proxy
        url: http://loki.logging-demo.svc.cluster.local:80
        isDefault: true
        jsonData:
          maxLines: 1000
