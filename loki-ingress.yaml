apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loki-ingress
  namespace: logging-demo
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: loki.tu-dominio.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loki
                port:
                  number: 80
